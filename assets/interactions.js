(function(){"use strict";window.setStatus=function(status){document.querySelectorAll("[data-status]").forEach(function(el){if(el.tagName==="BUTTON")return;el.classList.toggle("is-filtered",status!=="all"&&el.getAttribute("data-status")!==status)});document.querySelectorAll(".controls button[data-status]").forEach(function(b){b.classList.toggle("active",b.getAttribute("data-status")===status)})};function clear(){document.querySelectorAll(".node,.edge").forEach(function(el){el.classList.remove("is-related-dim")})}document.addEventListener("DOMContentLoaded",function(){document.querySelectorAll(".node[data-id]").forEach(function(node){node.addEventListener("mouseenter",function(){var id=node.getAttribute("data-id"),related=new Set([id]);document.querySelectorAll(".edge").forEach(function(edge){if(edge.getAttribute("data-source")===id||edge.getAttribute("data-target")===id){related.add(edge.getAttribute("data-source"));related.add(edge.getAttribute("data-target"))}});document.querySelectorAll(".node").forEach(function(n){if(!related.has(n.getAttribute("data-id")))n.classList.add("is-related-dim")});document.querySelectorAll(".edge").forEach(function(edge){if(!related.has(edge.getAttribute("data-source"))||!related.has(edge.getAttribute("data-target")))edge.classList.add("is-related-dim")})});node.addEventListener("mouseleave",clear)})})})();
